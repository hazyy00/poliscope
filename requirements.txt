# PoliScope — Python 데이터 수집 스크립트 의존성
# pip install -r requirements.txt

# HTTP
httpx==0.27.0          # 비동기 HTTP 클라이언트 (국회 API 수집)
aiohttp==3.9.5         # 비동기 HTTP (보조)

# 데이터 처리
pandas==2.2.2          # 데이터 조작
python-dotenv==1.0.1   # 환경변수 로딩

# Supabase
supabase==2.4.6        # Supabase Python 클라이언트
postgrest==0.16.7      # PostgreSQL REST 클라이언트

# AI
anthropic==0.29.0      # Claude API (배치 요약 생성)

# 스케줄링 (로컬 테스트용, 프로덕션은 Vercel Cron)
schedule==1.2.2

# 검증/유틸
pydantic==2.7.4        # 데이터 검증
loguru==0.7.2          # 로깅
tqdm==4.66.4           # 진행률 표시

# 텍스트 처리
konlpy==0.6.0          # 한국어 형태소 분석 (선택적)
